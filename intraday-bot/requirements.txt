alpaca-py>=0.30
python-dotenv>=1.0
pytz>=2024.1
# Optional — only needed when data_provider="ibkr" in config.json
ib_insync>=0.9.86
# Dashboard server (scripts/dashboard.py)
fastapi>=0.110
uvicorn[standard]>=0.29
# GUNS scanner — float lookup + catalyst news classifier
# (scripts/guns_float_lookup.py, scripts/guns_catalyst_classifier.py).
# Optional: scanner degrades gracefully (CAUTION flags) if not installed.
yfinance>=0.2.40
# Bars storage — Parquet read/write via resources/bars_store.py.
# Canonical format for data/bars/. Lazy-imported (only triggers when
# a strategy actually reads/writes historical bars).
pyarrow>=15
# System-tray status icon (dashboard/tray_status.py). Optional polish —
# the bot runs fine without it; this gives an at-a-glance ingest-state
# indicator in the Windows tray. Runs anywhere with a desktop session.
pystray>=0.19
Pillow>=10
